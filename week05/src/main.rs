mod enums;
mod generics;
mod dynamic;

fn main() {
    // Polymorphism
    // The ability to present the same interface for different underlying forms or data types.

    // Types of Polymorphism
    // Ad-Hoc Polymorphism
    // Define a common interface for a set of individually specified types

    // Parametric Polymorphism
    // Use a generic type to define a piece of code, then instantiate with specific types when needed.
    // Key difference to Ad-Hoc polymorphism. parametric polymorphic definitions behave identically
    // to each other while Ad-hoc definitions are distinct per type.

    // Subtyping
    // Classes inherit behaviour from parent classes





    // Polymorphism in Rust

    // Generics

    // Dynamic Dispatch
}
